<?php

    $qry[0]['apikey'] = ''; // enter your apikey
    $qry[0]['debug'] = false; // return errors
    $qry[0]['sozial'] = false; // load players sozial media links

    $qry['gear']['type'] = false; // load players gear type
    $qry['gear']['product'] = false; // load players gear product name
    $qry['gear']['url'] = false; // load players gear url
    $qry['gear']['picurl'] = false; // load players gear picture url (230x230px)

    $qry['settings']['video'] = false; // load players video settings
    $qry['settings']['mouse'] = false; // load players mouse settings
    $qry['settings']['keybinds'] = false; // load players keybinds settings
    $qry['settings']['crosshair'] = false; // load players crosshair settings
    
?>